x-go-base: &node-config
  build:
    context: .
    dockerfile: Dockerfile

services:
  node1:
    <<: *node-config
    ports:
      - '60000:50051'
    volumes:
      - node1-data:/store
  node2:
    <<: *node-config
    ports:
      - '60001:50051'
    volumes:
      - node2-data:/store
  node3:
    <<: *node-config
    ports:
      - '60002:50051'
    volumes:
      - node3-data:/store

volumes:
  node1-data:
  node2-data:
  node3-data:
